[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-test-scenarios"
version = "0.2.0"
description = "Conversation scripts, tester personas, edge cases, and golden datasets"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["test-scenarios","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-test-scenarios"
Repository = "https://github.com/mxn2020/minions-test-scenarios"

[tool.hatch.build.targets.wheel]
packages = ["minions_test_scenarios"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
